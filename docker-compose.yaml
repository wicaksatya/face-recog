version: '3.8'

services:
  web:
    image: swicaksana/face_recog:latest
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - ./images:/app/images
    environment:
      - FLASK_ENV=development
    command: python app.py
